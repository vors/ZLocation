install:
  - cinst pester

build: false

test_script:
  - ps: |
        psake AppveyorTest
        if ($psake.build_success -eq $false) { 
            exit 1 
        } else { 
            exit 0 
        }
