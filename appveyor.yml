install:
  - cinst pester

build: false

test_script:
  - ps: |
        Invoke-Psake AppveyorTest
        if ($psake.build_success -eq $false) { 
            exit 1 
        } else { 
            exit 0 
        }
        